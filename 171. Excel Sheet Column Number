class Solution {
    public int titleToNumber(String columnTitle) {
        char ch[] = columnTitle.toCharArray();
        int res = 0;
        for(int i=0;i<ch.length;i++){
            res = res*26 + ch[i]-'A'+1;
        }
        return res;
    }
}
