class Solution {
    public void reverseString(char[] s) {
        int n = s.length, m = n/2;
        char ch;
        for(int i=0;i<m;i++){
            ch = s[i];
            s[i] = s[n-i-1];
            s[n-i-1] = ch;
        }
        
    }
}
